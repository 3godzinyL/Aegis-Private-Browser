{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set for the Aegis manager window: core app/window plus the app's own #[tauri::command] handlers (which are always reachable). No filesystem, shell, or network plugin permissions are granted.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-minimize",
    "core:window:allow-start-dragging"
  ]
}
