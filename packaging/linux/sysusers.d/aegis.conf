# Dedicated unprivileged system user for the Aegis daemon (spec §10: not root).
# Created at install time by systemd-sysusers.
u aegis - "Aegis Private Browser daemon" /var/lib/aegis -
# The `aegis` group also gates access to the control socket; add desktop users
# who may drive Aegis to this group.
g aegis -
